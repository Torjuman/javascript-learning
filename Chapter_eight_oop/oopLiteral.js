
var rect = {

    // Here we can declare all the properties which an object can have.
    // First we have to name a key then assign its value.

    width: 100,
    hiegth: 50,

    // We can also define a function in Object.
    calculateArea: function () {
        console.log('I am a rectangle from rect Object....')
    }

}

rect.calculateArea()
